{
  "name": "slack-openai-assistant-ts",
  "private": true,
  "scripts": {
    "dev": "npx serverless offline --noPrependStageInUrl",
    "localtunnel": "npx localtunnel -p 3000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@slack/bolt": "^3.17.1",
    "dotenv": "^16.4.1",
    "esbuild": "^0.20.0",
    "node-fetch": "^3.3.2",
    "openai": "^4.26.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.16",
    "serverless-esbuild": "^1.51.0",
    "serverless-offline": "^13.3.3",
    "typescript": "^5.3.3"
  }
}
