{
  "name": "slack-openai-assistant-ts",
  "private": true,
  "scripts": {
    "deploy": "npx serverless deploy --stage dev",
    "dev": "npx serverless offline --stage local --noPrependStageInUrl",
    "localtunnel": "npx localtunnel --port 3000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@slack/bolt": "^3.17.1",
    "esbuild": "^0.20.0",
    "node-fetch": "^3.3.2",
    "openai": "^4.26.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.16",
    "serverless-esbuild": "^1.51.0",
    "serverless-offline": "^13.3.3",
    "typescript": "^5.3.3"
  }
}
